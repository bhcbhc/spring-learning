import React, { FC } from 'react';

interface WorkPlaceDataProps {}

const WorkPlace: FC<WorkPlaceDataProps> = props => {
  const {} = props;
  return <div>WorkPlace</div>;
};

export default WorkPlace;
